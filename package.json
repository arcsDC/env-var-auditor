{
  "name": "env-var-auditor",
  "version": "1.0.0",
  "description": "A CLI tool that audits .env files for missing, unused, and duplicate variables against a schema.",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  }
}
